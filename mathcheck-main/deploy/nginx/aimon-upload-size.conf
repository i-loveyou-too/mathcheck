# Include this file from nginx's http or aimon.teamzsoft.com server context.
# FastAPI accepts listening MP3 files up to 100MB; the extra 10MB covers
# multipart headers and keeps nginx from rejecting the request first.
client_max_body_size 110m;
